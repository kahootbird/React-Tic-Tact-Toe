import React from 'react';
//import Block from "./Block.js"

const UserBoard = (props) => {

    //console.log("PROPS:" + props)
        return (
            <button type="text" onChange={props.changed}/>

            
        )
    
}
export default UserBoard;